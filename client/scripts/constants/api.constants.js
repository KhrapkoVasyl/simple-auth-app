export const API_URI = 'http://localhost:8080/api/v1';

export const apiEndpoints = {
  signUp: `${API_URI}/auth/sign-up`,
  signIn: `${API_URI}/auth/sign-in`,
  profile: `${API_URI}/auth/profile`,
  users: `${API_URI}/users`
};
